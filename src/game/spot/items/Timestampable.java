package game.spot.items;

/**
 * The Timestampable interface used to sort objects that implements it by
 * timestamp
 */
public interface Timestampable {

	/**
	 * Get the timestamp of the object
	 * 
	 * @return the object's timestamp
	 */
	public String getTimestamp();

}
